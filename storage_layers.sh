# aufs, overlay1, overlay2
docker container inspect web1

readonly layers image layers
R/W thin layer container layer
# run 2 containers

docker container run -d -P nginx
docker container run -d -P nginx
 

# To see the size of container
docker container ps -s